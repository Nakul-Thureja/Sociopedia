<h1>Sociopedia- Client</h1>

Install the node modules:
`npm install`

To run the server run the command:
`npm run start`

Client should be running on port 3000
The website should be accessible on `localhost:3000`